class SubscriptionModel {
  final String title;
  final double price;
  bool isSelected;

  SubscriptionModel({
    required this.title,
    required this.price,
    required this.isSelected,
  });
}
